services:
  - type: web
    name: robotics-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    build:
      environment:
        PYTHON_VERSION: 3.10.13
